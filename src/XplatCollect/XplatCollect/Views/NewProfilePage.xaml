<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
              prism:ViewModelLocator.AutowireViewModel="True"
    xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Name, Mode=OneWay}"
             x:Class="XplatCollect.Views.NewProfilePage"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView">
  <ContentPage.Content>
        <StackLayout>
            <yummy:PancakeView CornerRadius="26" HeightRequest="86" WidthRequest="86">
                <Image Aspect="AspectFill"
                       BackgroundColor="#CCC"
                       Source="{Binding Avatar}" />
                <yummy:PancakeView.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding TakePictureCommand}" />
                </yummy:PancakeView.GestureRecognizers>
            </yummy:PancakeView>
            <Entry Placeholder="Name" Text="{Binding Name}" />
            <Entry Placeholder="Bio" Text="{Binding Bio}" />
            <Button Text="Salvar" Command="{Binding SaveProfileCommand}" />
        </StackLayout>
  </ContentPage.Content>
</ContentPage>